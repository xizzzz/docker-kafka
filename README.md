kafka-docker
============

Dockerfile for [Apache Kafka](http://kafka.apache.org/)

It contains a zookeeper and three kafka brokers.

##Usage

Start a cluster:

- ```docker-compose up -d ```

Destroy a cluster:

- ```docker-compose stop```
